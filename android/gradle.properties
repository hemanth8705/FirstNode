org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Disable Kotlin incremental compilation caches. On Windows these caches can fail
# to close ("Could not close incremental caches"), usually due to antivirus/file
# locking, breaking the build. Turning them off trades a little build speed for
# reliability. Safe to re-enable if the environment no longer locks build files.
kotlin.incremental=false
